{
  "PostToolUse": [
    {
      "matcher": {
        "tools": ["Read", "Grep"]
      },
      "hooks": [
        {
          "type": "prompt",
          "prompt": "When estimating tasks, consider complexity, dependencies, and break down into manageable subtasks."
        }
      ]
    }
  ]
}
