{
  "description": "Blockchain Agent - Smart contract security scanning",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/scripts/security-scan.py\"",
            "timeout": 15
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "For security token contracts: NO DEX/CEX integration, NO public trading, require KYC, require whitelist, require pause mechanism."
          }
        ]
      }
    ]
  }
}
