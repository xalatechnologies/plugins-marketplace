{
  "description": "Tauri Agent - Desktop app patterns",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "For Tauri commands: use async for I/O, return Result<T,E>, configure security capabilities."
          }
        ]
      }
    ]
  }
}
