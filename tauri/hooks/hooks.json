{
  "PostToolUse": [
    {
      "matcher": {
        "tools": ["Write", "Edit"]
      },
      "hooks": [
        {
          "type": "prompt",
          "prompt": "When creating Tauri commands, use async for I/O, return Result<T, E> for errors, configure capabilities for security."
        }
      ]
    }
  ]
}
