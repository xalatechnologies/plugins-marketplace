{
  "description": "Backend Agent - API security and validation checks",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "If this is an API endpoint, verify: input validation, proper error handling, authentication checks."
          }
        ]
      }
    ]
  }
}
