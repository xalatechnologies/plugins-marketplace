{
  "PostToolUse": [
    {
      "matcher": {
        "tools": ["Write", "Edit"]
      },
      "hooks": [
        {
          "type": "prompt",
          "prompt": "When creating API endpoints, ensure proper validation, error handling, authentication checks, and follow REST best practices."
        }
      ]
    }
  ]
}
