{
  "PostToolUse": [
    {
      "matcher": {
        "tools": ["Write", "Edit"]
      },
      "hooks": [
        {
          "type": "prompt",
          "prompt": "When creating database schemas or migrations, consider RLS policies for security, proper indexing, and TypeScript type generation."
        }
      ]
    }
  ]
}
