{
  "PostToolUse": [
    {
      "matcher": {
        "tools": ["Write", "Edit"]
      },
      "hooks": [
        {
          "type": "prompt",
          "prompt": "When creating or modifying code, consider test coverage. Suggest unit tests for new functions and E2E tests for user flows."
        }
      ]
    }
  ]
}
